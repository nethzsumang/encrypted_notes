import 'package:flutter/material.dart';

class AccountPreparation extends StatelessWidget {
  const AccountPreparation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Hey!');
  }

}